const windowHeight = window.innerHeight;
const headerHeight = 56
const navHeight = 200;
const extraPadding = 48;
export const mainHeightAdjustable = { 
    height: (windowHeight - navHeight - headerHeight - extraPadding)
};